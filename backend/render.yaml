services:
  - type: web
    name: personifid-backend
    env: python
    buildCommand: "pip3 install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0